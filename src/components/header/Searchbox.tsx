import styled from "styled-components";

const StyledSearchbox = styled.div`
  display: flex;
  align-items: center;
  gap: 1rem;
`;

const Searchbox = () => {
  return <StyledSearchbox>Searchbox Input</StyledSearchbox>;
};

export default Searchbox;
